# Reproducibility Guide (ns-3)

## Environment
- Tested on Ubuntu Linux
- ns-3 built via `./ns3 configure && ./ns3 build`

## Quick Build (ns-3 workspace)
```bash
cd ~/ns-3
./ns3 clean
./ns3 configure
./ns3 build
